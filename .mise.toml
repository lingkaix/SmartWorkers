# Generated by skills/general/workspace-setup/scripts/init_workspace.sh

[tools]
node = "25"
bun = "latest"
python = "3.14"
uv = "latest"

[tasks.setup]
description = "Install toolchain, init venv, and sync Python deps (uv)"
run = [
  "mise install",
  "uv venv --seed",
  "uv sync",
]

[env]
TEST_VAR = true
CODEX_SANDBOX_NETWORK_DISABLED = false