# Generated by skills/general/workspace-setup/scripts/init_workspace.sh

[project]
name = "smartworkers"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
# Add indexes, dev-dependencies, and resolution settings here as needed.
