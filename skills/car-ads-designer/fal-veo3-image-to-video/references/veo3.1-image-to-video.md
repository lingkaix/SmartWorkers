# fal-ai/veo3.1/image-to-video notes

Use this file as a local, versioned note for the model’s request/response schema.

Source (upstream): `https://fal.ai/models/fal-ai/veo3.1/image-to-video/llms.txt`

If the API schema changes or the script needs new fields, open the upstream `llms.txt` and update this file with:
- Required / optional request fields (including exact names)
- Response shape (where video URLs/data are returned)
- Any constraints (allowed ranges, enums, etc.)

