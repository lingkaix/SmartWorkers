interface:
  display_name: "Workspace Setup"
  short_description: "Standardize repo toolchain (mise)"
  default_prompt: "Use $workspace-setup to standardize this repo’s dev+CI toolchain (mise: Node/Python/uv) and generate the baseline workspace skeleton."
